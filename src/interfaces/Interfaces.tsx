interface Event {
    _id: number,
    title: string,
    description: string,
    date: Date,
    expositor: string,
    v: number
}

interface CustomColor {
    transparent: string,
}

interface BottomTabIcons {
    iconSize: number,
    txtSize: number,
    color: string
}

