import { Platform } from "react-native";

export const colors: any = {
    light: {
        color: {
            primary: '#00624e',
        }
    },
    dark: {
        colors: {
            primary: '#00624e'
        }
    }
}