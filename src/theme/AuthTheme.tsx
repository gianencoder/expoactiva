import { StyleSheet } from "react-native";

export const authStyle = StyleSheet.create({

    authCard: {
        flex: 1,
        padding: 10
    },
    title: {
        fontSize: 30,
        fontWeight: '400',
    }
})