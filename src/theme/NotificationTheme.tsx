import { StyleSheet } from "react-native";

export const notiTheme = StyleSheet.create({
    container: {
        flex: 1
        , justifyContent: 'center'
        , alignItems: 'center'
        , gap: 15
    }
})